document.addEventListener('DOMContentLoaded', () => {
  // Reserved for future interactive behaviors.
  // Keeping a small hook here ensures assets are wired correctly.
  console.log('Learning App UI initialized');
});
